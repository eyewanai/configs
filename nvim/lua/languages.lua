-- Языки, для которых ставятся Treesitter-парсеры.
--
-- Вынесено в отдельный модуль, потому что список нужен в двух местах: сам
-- конфиг ставит парсеры при старте, а install.sh дожидается их сборки при
-- развёртывании на новой машине. Дублировать его в скрипте — верный способ
-- получить расхождение.
return {
  "bash", "go", "gomod", "gowork", "json", "lua", "markdown",
  "markdown_inline", "query", "toml", "vim", "vimdoc", "yaml",
}
